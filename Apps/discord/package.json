{
  "name": "discord",
  "version": "0.7.0",
  "description": "This app is a discord app that shows your call stats on the DeskThing",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "build": "npm run build-discordweb && esbuild index.js --bundle --platform=node --outfile=builds/index.js && node buildHelper.cjs",
    "build-discordweb": "cd discordweb && npm run build",
    "test": "echo \"Error: no test specified\" && exit 1",
    "postversion": "node -e \"const fs = require('fs'); const manifest = require('./public/manifest.json'); const pkg = require('./package.json'); if (manifest.version !== pkg.version) { manifest.version = 'v' + pkg.version; fs.writeFileSync('./public/manifest.json', JSON.stringify(manifest, null, 2), 'utf8'); console.log('Updated manifest version to ' + pkg.version); }\" && npm install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "esbuild": "^0.21.5"
  },
  "dependencies": {
    "discord-rpc": "^4.0.1"
  }
}
